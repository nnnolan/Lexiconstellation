// Add any interactive JavaScript here
// For now, the CSS animations handle everything

document.addEventListener('DOMContentLoaded', () => {
    console.log('Lexiconstellation loaded');
    
    // You can add click handlers or other interactions here later
    // For example, clicking to proceed to the main app
});